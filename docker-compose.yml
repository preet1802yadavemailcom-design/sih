services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: apix
      POSTGRES_USER: apix
      POSTGRES_PASSWORD: apix_dev_only
    ports:
      - "5432:5432"
    volumes:
      - apix_pgdata:/var/lib/postgresql/data
volumes:
  apix_pgdata:
